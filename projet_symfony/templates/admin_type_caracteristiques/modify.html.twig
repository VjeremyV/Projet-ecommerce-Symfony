{% extends 'admin/template.html.twig' %}

{% block title %}Ajouter un Type de caractéristique{% endblock %}

{% block body %}
    <div class="example-wrapper p-5">
        <h1 class="py-5">
            Modifier un type de caractéristique
        </h1>
        <form action="">
            <div class="row form-group">
                <label for="year" class="col-sm-6 col-form-label">Nom du type de la caractéristique : </label>
                <div class="col-sm-6">
                    <select type="text" class="form-control" id="year_search" name="year_search">
                        <option></option>
                        {% for gettypeCaracteristique in gettypeCaracteristiques %}
                            <option {{ (gettypeCaracteristique == searchTypeCaracteristiques ? 'selected')}}> {{gettypeCaracteristique}} </option>
                        {%endfor%}
                    </select>
                </div>
            </div>
        </form>
        <form action="">
            <label for="search_city">Rechercher un type de la caractéristique
                <input type="search"  id="search_city" name="search_city">
            </label>
            <button type="submit">Envoyer</button>
        </form>
        <ul class="list-group ">
            {% for typeCaracteristique in typeCaracteristiques %}
                <li class="list-group-item list-group-item-action d-flex justify-content-between">{{typeCaracteristique.nom}}
{#                    <a href="{{ path('update_conference', { id: conference.id }) }}" class="btn btn-primary">Editer</a>#}
                </li>
            {% endfor %}
        </ul>
    </div>
    <div class="col-12">
        {% if previous >= 0 %}
            <a href="{{ path('TypeCaracteristiques', {offset: previous }) }}" class="btn btn-sm btn-primary"> previous </a>
        {% endif %}
        Il y a {{ typeCaracteristiques|length~' type de caracteristique'~( typeCaracteristiques|length > 1 ? 's' : '') }}
        {% if next < typeCaracteristiques|length %}
            <a href="{{ path('TypeCaracteristiques', {offset: next }) }}" class="btn btn-sm btn-primary">next </a>
        {% endif %}
    </div>
{% endblock %}
